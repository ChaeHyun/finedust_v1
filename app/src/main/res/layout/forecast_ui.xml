<?xml version="1.0" encoding="utf-8"?>
<layout>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <include
            android:id="@+id/layout_info_zero"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"

            android:layout_centerHorizontal="true"
            android:layout_marginBottom="0dp"
            android:layout_marginTop="5dp"
            layout="@layout/forecast_info_today" />

        <include
            android:id="@+id/layout_info"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/layout_info_zero"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="0dp"
            layout="@layout/forecast_info_tomorrow" />

        <include
            android:id="@+id/layout_reason"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/layout_info"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="0dp"
            layout="@layout/forecast_reason" />

        <include
            android:id="@+id/layout_states"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/layout_reason"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="0dp"
            layout="@layout/forecast_states" />
    </RelativeLayout>

</ScrollView>
</layout>
