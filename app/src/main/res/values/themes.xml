<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base Application theme. -->
    <style name="Base.Theme.BreatheInAndOut" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>

    <!-- This will be applied to application in the manifest.xml -->
    <style name="Theme.BreatheInAndOut" parent="Base.Theme.BreatheInAndOut" />

    <!-- For Activity -->
    <style name="Theme.BreatheInAndOut.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:screenOrientation">fullSensor</item>
    </style>

    <style name="ThemeOverlay.AppBar" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />
    <style name="ThemeOverlay.Toolbar" parent="ThemeOverlay.MaterialComponents.Dark" />

    <style name="Theme.SettingScreenColorTheme" parent="PreferenceThemeOverlay">
        <item name="colorAccent">@color/darkGray</item>
    </style>

    <!-- Splash Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorPrimaryVariant</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icon_splash</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>

        <item name="postSplashScreenTheme">@style/Theme.BreatheInAndOut.NoActionBar</item>
    </style>

</resources>