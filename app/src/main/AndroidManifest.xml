<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.breatheinandout">

    <!-- Permissions for using Application -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/main_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/main_icon"
        android:supportsRtl="true"
        android:name="android.support.multidex.MultiDexApplication"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name="ch.breatheinandout.view.MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ch.breatheinandout.view.SearchAddressActivity" />
        <activity android:name="ch.breatheinandout.view.ChangeGradeActivity" />

        <!-- About Widgets -->
        <receiver
            android:name="ch.breatheinandout.widget.WidgetDark"
            android:label="들숨날숨(어두운버전)">
            <intent-filter>
                <!-- Essential Parameter -->
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />

                <!-- Custom actions -->
                <action android:name="ch.widget_action.WIDGET_DARK_RESPONSE_FROM_SERVER" />
                <action android:name="ch.bootcompleted.control.BootCompletedReceiver" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_dark_info" />
        </receiver>

        <activity android:name="ch.breatheinandout.widget.WidgetDarkConfigureActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name="ch.breatheinandout.widget.WidgetWhite"
            android:label="들숨날숨(밝은버전)">
            <intent-filter>
                <!-- Essential Parameter -->
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />

                <!-- Custom actions -->
                <action android:name="ch.widget_action.WIDGET_WHITE_RESPONSE_FROM_SERVER" />
                <action android:name="ch.bootcompleted.control.BootCompletedReceiver" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_white_info" />
        </receiver>

        <activity android:name="ch.breatheinandout.widget.WidgetWhiteConfigureActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <service android:name="ch.breatheinandout.service.WidgetDarkService"
            android:exported="false">
            <intent-filter>
                <action android:name="ch.widget_action.WIDGET_DARK_GETDATA_FROM_SERVER" />
            </intent-filter>
        </service>
        <service android:name="ch.breatheinandout.service.WidgetWhiteService"
            android:exported="false">
            <intent-filter>
                <action android:name="ch.widget_action.WIDGET_WHITE_GETDATA_FROM_SERVER" />
            </intent-filter>
        </service>

        <receiver
            android:name="ch.breatheinandout.service.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMMPLETED" />
            </intent-filter>
        </receiver>

        <activity android:name="ch.breatheinandout.view.Splash"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>